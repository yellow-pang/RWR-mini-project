# Express API 서버 주소 (선택)
# - 미설정 시: 상대경로 /api 사용 → 프로덕션(nginx 프록시) 및 로컬 Vite proxy 모두 자동 동작
# - 로컬에서 Vite proxy 없이 직접 서버를 가리킬 때만 설정
# VITE_API_BASE_URL=http://localhost:3000

# 카카오맵 JavaScript API 키 (https://developers.kakao.com 에서 발급)
VITE_KAKAO_MAP_KEY=your_kakao_api_key_here
